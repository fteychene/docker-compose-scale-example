version: "2"

services: 
  mynodeapp:
    build: 
      context: ./app
      dockerfile: Dockerfile 
    ports: 
      - "3333:3333"
    networks:
      - mynetwork

  nginx: 
    build: 
      context: ./nginx
      dockerfile: Dockerfile 
    ports:
      - "80:80"
      - "443:443"
    networks:
      - mynetwork
    links:
      - mynodeapp

  
networks: 
  mynetwork:
